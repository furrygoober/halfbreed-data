## adding cards

-- create set
vim edn/sets.edn

-- create cycle
vim edn/cycles.edn

-- create update cards in set
### not spaces should be a - in id names
vim edn/set-cards/{SET_NAME.edn}

-- create the file for the card
vim edn/cards/{CARD-ID}.edn

-- update under halfbreeds:
./resources/public/i18n/en.ftl

-- create raw file
clj -X:combine

-- from halfbreeds fetch the updated card info
lein fetch --local ../halfbreed-data
lein fetch -d --no-card-images -r furrygoober/halfbreed-data
